<html>
<head>
<title>My Webstore</title>
<%= stylesheet_link_tag "main" %>
</head>
<body>
<h1> Welcome to my webstore!</h1>
<h3> Written by <%= @name %> </h3>

<h2>Take the poll! What is your favorite color?</h2>

<!-- HTML Form -->
<form action="/poll" method="POST">
    <label>Color:</label>
    <input name="color" type="text">
    <input type="submit">
</form>

<hr/>

<table>
<thead>
<th>Name</th><th>Weight</th><th>Color</th>
</thead>
    <tbody>
        <% @cats.each do |cat| %>
        <tr>
            <td><%= cat[:name] %></td>
            <td><%= cat[:weight] %></td>
            <td><%= cat[:color] %></td>
        <%end %>
    </tbody>
</table>
Visit my <%= link_to "about page", "/about"%>
</body>
</html>
